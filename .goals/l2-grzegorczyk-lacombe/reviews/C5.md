verdict: passes

# C5 — l2_neg_gl (and its private helper isComputableSeqReal_neg)

## Summary

C5 (`l2_neg_gl`) is a clean, named, sorry-free lemma that correctly proves `IsGLComputable f → IsGLComputable (-f)` for `f : C(Set.Icc α β, ℝ)`. The Lean file (lines 421-447 for `l2_neg_gl`, 372-403 for the private helper `isComputableSeqReal_neg`) type-checks cleanly under `lake env lean` (exit 0), contains zero `sorry`, and `#print axioms` emits only the three standard axioms `[propext, Classical.choice, Quot.sound]`. The modulus reuse claim — `df` works for `-f` unchanged because `|(-f) x − (-f) y| = |f x − f y|` — is sound and the supporting Lean tactic chain is correct. The helper `isComputableSeqReal_neg` builds the witness `r(n,k) := -ra(n,k)` via `IsComputableSeqRat.neg` and applies `isComputableSeqReal_of_effectiveConvergence` with effective modulus `e(n,N) := N`. No blocking issues found.

## refine 5-tuple unpacking (modulus reuse)

The `IsGLComputable` predicate has five components. The hypothesis is destructured via `obtain ⟨hf_seq, df, hdf_comp, hdf_pos, hdf_mod⟩ := hf` — five binders. The `refine ⟨fun x hx => ?_, df, hdf_comp, hdf_pos, ?_⟩` then provides:
1. `fun x hx => ?_` — Clause (i) hole
2. `df` — witness for `∃ d` (reused literally from `f`)
3. `hdf_comp` — `Computable df` (reused)
4. `hdf_pos` — `∀ N, 0 < df N` (reused)
5. `?_` — Clause (ii) hole

This works because the existential witness only needs to satisfy the same three conjuncts for the new function — which is what the proof verifies.

## Clause (i) — sequential computability via helper

`h_f := hf_seq x hx : IsComputableSeqReal (fun n => f (x n))` extracted from clause-(i) of `f`.

The `h_eq` step `(fun n => (-f) (x n)) = (fun n => - (f (x n)))` discharges via `funext n; show - f (x n) = - f (x n); rfl` — works because `(-f) (x n)` unfolds to `- f (x n)` via the `Neg` instance on `C(X, ℝ)` (`Mathlib/Topology/ContinuousMap/Algebra.lean:152` additivized) and the RHS `-(f (x n))` is `- f (x n)` by parser convention. Verified by independent type-check.

After `rw [h_eq]`, the goal is `IsComputableSeqReal (fun n => - (f (x n)))`, dispatched by `isComputableSeqReal_neg h_f`.

## Modulus implication — invariance

After `intro N x y hxy` with `hxy : |(x:ℝ) - (y:ℝ)| ≤ 1/(df N : ℝ)`, goal is `|(-f) x - (-f) y| ≤ 1/2^N`.

The `h_eq_neg : (-f) x - (-f) y = - (f x - f y)` discharges via `show (- f x) - (- f y) = - (f x - f y); ring` — `ring` over ℝ closes the standard algebra identity.

The `h_abs : |(-f) x - (-f) y| = |f x - f y|` discharges via `rw [h_eq_neg, abs_neg]`. `abs_neg : |-a| = |a|` is generated via `@[to_additive]` from `mabs_inv` at `Mathlib/Algebra/Order/Group/Unbundled/Abs.lean:76` — verified.

After `rw [h_abs]`, goal becomes `|f x - f y| ≤ 1/2^N`, exactly `hdf_mod N x y hxy`'s type. ✓

## Inside isComputableSeqReal_neg

Witness assembly via `isComputableSeqReal_of_effectiveConvergence` (L1:157) with three subgoals:

1. **`IsComputableDoubleSeqRat (fun p => - ra p)`** via `hra_flat.neg` where `hra_flat : IsComputableSeqRat (fun n => ra (Nat.unpair n)) := hra` — definitional coercion from `IsComputableDoubleSeqRat`, then `IsComputableSeqRat.neg` (L1:426) gives `IsComputableSeqRat (fun n => - ra (Nat.unpair n))`, definitionally equal to the target. Independent type-check confirmed.

2. **`Computable (fun p : ℕ × ℕ => p.2)`** via `Computable.snd` — direct match.

3. **Bound cascade**: `hk : k ≥ (n, N).2 = N` after `intro n N k hk` and unfolding. The `h_eq` `((- ra (n, k) : ℚ) : ℝ) - (- a n) = - (((ra (n, k) : ℝ)) - a n)` via `push_cast; ring` is sound (`push_cast` normalizes `((-q:ℚ):ℝ) = -((q:ℚ):ℝ)` via `Rat.cast_neg`; `ring` closes `(-x)-(-y) = -(x-y)`). The `h_abs` via `rw [h_eq, abs_neg]` discharges similarly.

   The bound `1/2^k ≤ 1/2^N` for `k ≥ N`:
   - `h_pow_le : 2^N ≤ 2^k` via `pow_le_pow_right₀ (by norm_num : (1:ℝ) ≤ 2) hk`.
   - `h_inv : 1/2^k ≤ 1/2^N` via `one_div_le_one_div_of_le hN_pos h_pow_le`.
   - `le_trans (hra_bnd n k) h_inv` chains `|ra(n,k) - a n| ≤ 1/2^k` with `1/2^k ≤ 1/2^N`. ✓

## Mathlib lemma name audit

All cited Mathlib symbols verified to exist with the asserted signatures:

| Symbol | Location | Verified signature |
|---|---|---|
| `IsComputableSeqRat.neg` | L1:426-433 | `IsComputableSeqRat r → IsComputableSeqRat (fun k => -r k)` |
| `isComputableSeqReal_of_effectiveConvergence` | L1:157-163 | as documented |
| `Computable.snd` | `Computability/Partrec.lean:288` | `Computable (fun p => p.2)` |
| `pow_le_pow_right₀` | `Algebra/Order/GroupWithZero/Unbundled/Basic.lean:446` | `1 ≤ a → m ≤ n → a^m ≤ a^n` |
| `one_div_le_one_div_of_le` | `Algebra/Order/Field/Basic.lean:69` | `0 < a → a ≤ b → 1/b ≤ 1/a` |
| `abs_neg` | `Algebra/Order/Group/Unbundled/Abs.lean:76` (via to_additive) | `|-a| = |a|` |
| `Neg` instance on `C(X, ℝ)` | `Topology/ContinuousMap/Algebra.lean:152` | additivized from `Inv` |
| `(-f) x = -(f x)` | `Topology/ContinuousMap/Algebra.lean:159` (`neg_apply` via to_additive) | `rfl` |
| `ring`, `push_cast`, `positivity` | universal tactics | n/a |

All present, all matching. ✓

## P-R Definition A faithfulness

P-R chapt0:504 says "$x \pm y$ … are computable over any computable rectangle on which they are continuous." This is the explicit closure under binary `+` and `-`. Unary negation `-f` is morally `0 - f` — a corollary of `x ± y` with one operand fixed to `0`. The L2 round elects to prove unary negation directly rather than derive it from a hypothetical `l2_sub_gl`; this is a reasonable design choice and the proof is independently sound. No P-R departure detected. ✓

## Concrete weaknesses (if any)

None blocking. Minor observations:
1. P-R citation is moral, not literal — chapt0:504 mentions `x ± y` (binary), not unary `-`. The two are mathematically equivalent and the design choice is honest.
2. The helper uses a NEW effective modulus `e := fun p => p.2` rather than reusing the (hypothetical) modulus from `hra_bnd`. This is correct because `hra_bnd` is the Def-5a bound, not an `effectiveConvergence`-shaped bound — there's no modulus to reuse. The `e := fun p => p.2` reflects the conversion: at level `N`, we need the original bound at level `k = N`.
3. `Computable d` reuse — `hdf_comp` certifies the new modulus directly because the new modulus IS the old `df`. Elegant.

## Verdict justification

The proof is correct. The mathematical core — modulus invariance under negation via `|(-f) x − (-f) y| = |f x − f y|` — is a true identity over ℝ, and the Lean tactic chain is sound. The sequential-computability dispatch through the inline helper is clean. The helper itself correctly applies `isComputableSeqReal_of_effectiveConvergence` with witness `-ra` and modulus `e(n,N) := N`, using `IsComputableSeqRat.neg` and a `pow_le_pow_right₀ + one_div_le_one_div_of_le` cascade. Independent verification: `lake env lean` exits 0, `#print axioms` emits only `[propext, Classical.choice, Quot.sound]`, all cited Mathlib lemmas exist with the claimed signatures, the `(-f) x = -(f x)` rfl-claim type-checks under independent example. The lemma is named, sorry-free, builds, and faithfully formalizes (the implicit) closure of P-R Definition A under unary negation.
