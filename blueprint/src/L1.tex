\chapter{L1 — Computable reals and computable sequences}

% P-R reference: Chapter 0, "Computability on the real line", literature/papers/PourEl-Richards-chapt0.md.
% Layer status: predicate \texttt{IsComputableSeqReal} done (0 sorries);
% \texttt{IsComputableSeqComplex}, \texttt{IsComputableReal}, and the
% countable-subfield structure still pending. Content migration deferred.

\begin{definition}
  \label{def:l1_isComputableSeqReal}
  \notready
  Predicate \texttt{IsComputableSeqReal}\,$: (\mathbb{N} \to \mathbb{R}) \to \texttt{Prop}$ on Mathlib's \texttt{Real}. Statement migration pending. P-R Ch.\,0:55.
\end{definition}

\begin{definition}
  \label{def:l1_isComputableReal}
  \notready
  Predicate \texttt{IsComputableReal}\,$: \mathbb{R} \to \texttt{Prop}$. A real $x$ is computable iff the constant sequence $\lambda n.\,x$ satisfies $\texttt{IsComputableSeqReal}$. P-R Ch.\,0:55.
\end{definition}
