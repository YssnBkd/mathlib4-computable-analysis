---
artifact: blueprint/src/L4.tex (theorem thm:l4_cmap_instance + proof block)
reviewed_at: 2026-06-04T00:00:00Z
verdict: passes
---

# Restatement
The proof block of `thm:l4_cmap_instance` (L4.tex:34-40) now carries
`\uses{lem:l4_cmap_axiom1, def:l1_isComputableSeqReal}` and the Linearity
paragraph (L4.tex:37) cites `\Cref{lem:l4_cmap_axiom1}`. The theorem's
statement-level `\uses{def:l3_computabilityStructure}` (L4.tex:31) was NOT
extended. `leanblueprint web` regenerates the graph and the lemma-to-theorem
edge appears as a SOLID arrow; the theorem renders blue / `#A3D6FF` (can_prove).

# Hypotheses (load-bearing steps)
- Lemma reference is proof-level, not statement-level — verified by direct
  Read of L4.tex:28-40 (the `\uses{lem:l4_cmap_axiom1,...}` is inside
  `\begin{proof}` at L4.tex:35). Status: our_construction (this edit).
- Solid edges in this dep graph correspond to proof-level `\uses`; dashed
  edges to statement-level `\uses`. Status: tacit convention, but
  empirically corroborated by every other edge in the rendered graph
  (e.g. all `def:l1_isComputableSeqRat -> lem:l1_isComputableSeqRat_*`
  statement-level uses render dashed; `lem:* -> thm:propA/propB` proof-level
  uses render solid).
- `def:l1_isComputableSeqReal` is `\leanok`. Status: cited
  (blueprint/src/L1.tex:35).
- `lem:l4_cmap_axiom1` has both `\leanok` statement and `\leanok` proof.
  Status: cited (L4.tex:8 statement, L4.tex:15 proof).

# Attacks attempted

## Citation check
- L4.tex:31 statement-level `\uses{def:l3_computabilityStructure}` — unchanged
  by this round; no claim of statement-level dependency on the lemma. PASS.
- L4.tex:35 proof-level `\uses{lem:l4_cmap_axiom1, def:l1_isComputableSeqReal}`
  — `lem:l4_cmap_axiom1` matches the label at L4.tex:6; `def:l1_isComputableSeqReal`
  matches the label at L1.tex:33. Both targets resolve. PASS.
- L4.tex:37 `\Cref{lem:l4_cmap_axiom1}` renders in
  blueprint/web/sect0005.html:252 as `<a href="sect0005.html#lem:l4_cmap_axiom1">Lemma 27</a>`.
  Hyperlink working. PASS.
- Edge present: `grep` on dep_graph_document.html:1123 confirms
  `"lem:l4_cmap_axiom1" -> "thm:l4_cmap_instance"` with NO `[style=dashed]`
  attribute — i.e. a solid edge, the proof-level marker. PASS.

## Hypothesis weakening
- *What if the lemma were moved to statement-level instead?* The instance's
  Lean signature is `... → ComputabilityStructure ℝ (C(...))`; the signature
  itself only mentions the bound parameter, not Axiom 1. The body assigns
  `axiom_linearity := isComputableSeqCMap_linearCombination ...`. So the
  semantic dependency on A1 lives in the BODY, not the type — proof-level is
  correct.  An adversarial counter-argument: "the type is a typeclass with A1
  as a field, so the field's witness is semantically part of the type". But
  in Lean's definitional equality, the field is only forced when projecting,
  and the elaborator does NOT need the lemma to type-check the signature. The
  leanblueprint convention (per `docs/BLUEPRINT-CONVENTIONS.md` rationale:
  statement-level = "needed to state the result", proof-level = "needed to
  prove the result") cleanly puts field implementations at proof-level. PASS.
- *What if I drop `def:l1_isComputableSeqReal` from the proof-level uses?*
  The proof prose at L4.tex:38-39 still mentions "computable sequence of
  reals" (Axiom 3) which depends on this predicate. The use is justified;
  not over-cited. PASS.

## Counterexample hunt
- Fragile step 1: dep graph styling convention. *Could a future leanblueprint
  upgrade change solid-vs-dashed semantics?* The current rendering is
  load-bearing for the "edge is proof-level" claim. However the LaTeX SOURCE
  placement (inside the proof env at L4.tex:34-40) is the authoritative
  signal that survives any rendering convention change — the graph styling
  is a visualization of this source-level fact. ROBUST.
- Fragile step 2: the missing direct edge
  `def:l3_computabilityStructure -> thm:l4_cmap_instance` in the graph.
  Despite the statement-level `\uses{def:l3_computabilityStructure}` at
  L4.tex:31, no corresponding edge appears in the rendered dep graph.
  Investigation: leanblueprint may suppress definition-→-theorem edges when
  the theorem only consumes the definition declaratively, or routes via
  transitive paths. The lemma `def:l3_computabilityStructure -> lem:l4_cmap_axiom1`
  IS rendered (dashed). This is a pre-existing leanblueprint emission quirk
  unrelated to this round's edit; it does not affect C3's pass status (C3
  asks for the lemma-to-theorem edge, which IS rendered). NOTED, NOT FATAL.

# Quantifier / edge / completeness checks
- Theorem fill color check: `thm:l4_cmap_instance` renders
  `color=blue, fillcolor="#A3D6FF"`. Per BLUEPRINT-CONVENTIONS.md, blue fill
  = `can_prove`: all proof-level dependency targets must be leanok. Targets:
  (a) `lem:l4_cmap_axiom1` — leanok (L4.tex:8); (b) `def:l1_isComputableSeqReal`
  — leanok (L1.tex:35). Both leanok. The theorem itself lacks a `\leanok`
  on its `\begin{proof}` block — correct (proof not yet formalized; A2/A3
  fields still carry `sorry` per goal C4 stated state). Blue fill is the
  correct color. PASS.
- Lemma fill color sanity: `lem:l4_cmap_axiom1` renders
  `color=green, fillcolor="#1CAC78"` = `fully_proved` (statement leanok +
  proof leanok + all ancestors leanok/definition). Consistent with the goal
  C1/C2 win state. PASS.
- Statement-level vs proof-level boundary: confirmed by `Read` of L4.tex:28-40
  that `lem:l4_cmap_axiom1` appears at line 35 (inside the proof env, the
  proof's `\uses` line) and does NOT appear on line 31 (the theorem's
  statement-level `\uses` line). The boundary at `\end{theorem}` / `\begin{proof}`
  between lines 33 and 34 cleanly separates them.

# Verdict reasoning
All five adversarial questions resolve in favor of the artifact:
1. Placement is provably proof-level (L4.tex:35 vs L4.tex:31 separation).
2. Placement is correct per leanblueprint convention (field assignment lives
   in instance body, not type signature).
3. Edge is rendered (dep_graph_document.html:1123) and SOLID (no
   `[style=dashed]`), matching the proof-level placement.
4. `\Cref` resolves to a working hyperlink (sect0005.html:252).
5. Blue fill is correct: both proof-level targets are leanok; the theorem's
   proof itself is unproved (no `\leanok` on the proof env), consistent with
   `can_prove` semantics.

No silent strengthening, no over-citing, no misplaced edge. C3 passes.

# Specific fixes required (if not passes)
None — verdict is passes.

# Minor non-blocking observation (out of C3's strict scope)
The absence of a directly-rendered `def:l3_computabilityStructure -> thm:l4_cmap_instance`
edge — despite L4.tex:31 declaring that statement-level dependency — is a
quirk of leanblueprint's emission. The transitive path
`def:l3_computabilityStructure -> lem:l4_cmap_axiom1 -> thm:l4_cmap_instance`
keeps the structural dependency visible. Worth filing as a leanblueprint
upstream observation but NOT a blocker for this round.
