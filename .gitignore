.venv/
**/__pycache__/
*.pyc
*.pyo

# LaTeX build artifacts
*.aux
*.log
*.out
*.toc
*.synctex.gz
*.fdb_latexmk
*.fls
*.bbl
*.blg

# Lean build artifacts
.lake/
build/

# Blueprint build outputs (added 2026-06-02)
blueprint/print/
blueprint/web/
blueprint/src/*.paux
blueprint/src/web.bbl

# OS
.DS_Store

# Local-only session JSONL copies
archive/raw/*.jsonl.tmp

# Local-only Claude Code overrides (commit settings.json, not the .local override)
.claude/settings.local.json

# Transient agent worktrees (managed by `git worktree`; harness-locked while in use)
.claude/worktrees/
