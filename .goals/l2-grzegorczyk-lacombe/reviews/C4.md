verdict: passes

# C4 — l2_sum_gl (and its private helper isComputableSeqReal_add)

## Summary

The artifact `ComputableAnalysis/L2/GrzegorczykLacombe.lean` (specifically `l2_sum_gl` at lines 298-360 and its private helper `isComputableSeqReal_add` at lines 227-266) cleanly proves criterion C4. The reviewer conducted citation, hypothesis-weakening, counterexample, and quantifier attacks; ran a full Mathlib lemma signature audit; verified P-R faithfulness to chapt0:504; confirmed the build is clean and the axiom dependency is `[propext, Classical.choice, Quot.sound]` only (no `sorryAx`). The iter-06 positivity-conjunct fix in `IsGLComputable` is correctly propagated into the modulus-cascade reasoning, and no vacuity loophole is reintroduced. The `+1` shift in `df (N + 1) * dg (N + 1)` is essential and correctly applied — dropping it would yield only `≤ 1/2^(N-1)`, not the required `≤ 1/2^N`.

## refine 5-tuple unpacking (line 304)

The predicate `IsGLComputable f` desugars (per lines 107-114) to:
```
(∀ x, IsComputableSeqReal (x⋅) → IsComputableSeqReal (f∘x))
  ∧ ∃ d, Computable d ∧ (∀ N, 0 < d N) ∧ ∀ N x y, hypothesis → conclusion
```

The 5-tuple `⟨fun x hx => ?_, fun N => df (N + 1) * dg (N + 1), ?_, ?_, ?_⟩` provides:
1. `fun x hx => ?_` — clause (i) closure proof obligation
2. `fun N => df (N + 1) * dg (N + 1)` — the witness `d : ℕ → ℕ` for the existential
3. `?_` — `Computable d`
4. `?_` — `∀ N, 0 < d N`
5. `?_` — modulus implication

The anonymous-constructor associativity flattens `(A ∧ ∃ d, B ∧ C ∧ D)` into the 5-tuple correctly — Lean's `⟨...⟩` parser handles nested `And.intro`/`Exists.intro` here exactly as it did for `l2_const_gl`/`l2_id_gl` (verified by clean type-check). ✓

## Clause (i) — sequential computability via helper (lines 305-315)

The `h_eq` step:
```lean
have h_eq :
    (fun n => (f + g) (x n)) =
      (fun n => f (x n)) + (fun n => g (x n)) := by
  funext n
  show f (x n) + g (x n) = f (x n) + g (x n)
  rfl
```

After `funext n`, the goal becomes `(f + g) (x n) = ((fun n => f (x n)) + (fun n => g (x n))) n`. The LHS reduces via `ContinuousMap.add_apply` (verified at `Mathlib/Topology/ContinuousMap/Algebra.lean:64` — `mul_apply : (f * g) x = f x * g x := rfl`, which under `@[to_additive]` generates `add_apply` also as `rfl`) to `f (x n) + g (x n)`. The RHS reduces via `Pi.add_apply` (also a `rfl`) to `f (x n) + g (x n)`. The `show` succeeds and `rfl` closes. ✓

Then `rw [h_eq]` rewrites the goal to `IsComputableSeqReal ((fun n => f (x n)) + (fun n => g (x n)))`, which the helper `isComputableSeqReal_add h_f h_g` discharges directly. ✓

## Positivity of d_sum (lines 319-321)

`Nat.mul_pos` in Mathlib has signature `0 < a → 0 < b → 0 < a * b`. The call `Nat.mul_pos (hdf_pos (N + 1)) (hdg_pos (N + 1))` produces `0 < df (N + 1) * dg (N + 1)`. ✓

Critically: this step is the precise consumer of the iter-06 predicate fix. Without `(∀ N, 0 < d N)` in the predicate, we'd have no `hdf_pos`/`hdg_pos` to extract, and we couldn't establish positivity of the product — the cascade in clause (ii) would collapse.

## Modulus implication — the cascade (lines 322-360)

**`one_div_le_one_div_of_le`** at `Mathlib/Algebra/Order/Field/Basic.lean:69`:
```
theorem one_div_le_one_div_of_le (ha : 0 < a) (h : a ≤ b) : 1 / b ≤ 1 / a
```

Application pattern at lines 335-337:
```lean
apply one_div_le_one_div_of_le
· exact_mod_cast h_df_pos      -- 0 < df (N + 1)
· exact_mod_cast h_df_le_prod  -- df (N + 1) ≤ df (N + 1) * dg (N + 1)
```

This concludes `1 / (df (N+1) * dg (N+1)) ≤ 1 / df (N+1)` in ℝ. The `exact_mod_cast` calls handle the Nat→ℝ casts. After `le_trans hxy ?_`, we obtain `|x − y| ≤ 1 / df (N+1)`. Symmetric for `dg`. ✓

**`Nat.le_mul_of_pos_right`** and **`Nat.le_mul_of_pos_left`**: verified signatures:
- `Nat.le_mul_of_pos_right (a : ℕ) (h : 0 < b) : a ≤ a * b` ✓
- `Nat.le_mul_of_pos_left (b : ℕ) (h : 0 < a) : b ≤ a * b` ✓

**Modulus calls at `N + 1`** (lines 345-348): `hdf_mod (N + 1) x y h_xy_le_df : |f x − f y| ≤ 1 / 2^(N+1)`. Symmetric for `g`. The `+1` shift is essential and correctly applied. ✓

**Triangle inequality** (`abs_add_le`, lines 354-356): signature `|a + b| ≤ |a| + |b|`. After `rw [h_eq_sum]`, the LHS becomes `|(f x − f y) + (g x − g y)|`, and `abs_add_le _ _` bounds it by `|f x − f y| + |g x − g y|`. ✓

**`h_2pow` arithmetic** (lines 357-359): `1/2^(N+1) + 1/2^(N+1) = 1/2^N`. After `rw [pow_succ]`: `2^(N+1) → 2^N * 2`, so LHS = `1/(2^N * 2) + 1/(2^N * 2) = 2/(2^N * 2)`, which equals `1/2^N` by cancellation. `ring` over ℝ (a `Field`) handles this. ✓

**Final `linarith`** (line 360): combines `h_tri`, `hf_bnd`, `hg_bnd`, `h_2pow` linearly to close
`|(f+g)x − (f+g)y| ≤ |fx − fy| + |gx − gy| ≤ 1/2^(N+1) + 1/2^(N+1) = 1/2^N`. ✓

## Inside isComputableSeqReal_add

**Witness**: `r(n,k) := ra(n,k) + rb(n,k)`. The flattening idiom `hra_flat : IsComputableSeqRat (fun n => ra (Nat.unpair n)) := hra` works because `IsComputableDoubleSeqRat r` (L1:97) is defined as `IsComputableSeqRat (fun n => r (Nat.unpair n))` — definitional equality. ✓ Then `hra_flat.add hrb_flat` (using `IsComputableSeqRat.add` at L1:255) provides the sum's computability. ✓

**Modulus**: `e(n,N) := N + 1`, witnessed `Computable` by `Computable.succ.comp Computable.snd`. ✓

**Bound cascade**: with `hk : k ≥ N + 1`:
1. `h_pow_le : (2 : ℝ)^(N+1) ≤ 2^k` via `pow_le_pow_right₀ (by norm_num : (1:ℝ) ≤ 2) hk`. ✓
2. After `rw [pow_succ] at h_pow_le`: `(2:ℝ)^N * 2 ≤ 2^k`.
3. After the show-rewrite to `2/2^k` and `rw [div_le_div_iff₀ hk_pos hN_pos]` (signature: `0 < b → 0 < d → (a/b ≤ c/d ↔ a*d ≤ c*b)`), the goal is `2 * 2^N ≤ 1 * 2^k`.
4. `linarith` closes via `h_pow_le`. ✓

**Final assembly**: `h_tri` via `abs_add_le _ _` after `rw [h_eq]`; `h_a`, `h_b` from `hra_bnd`/`hrb_bnd`; `h_inv_le` as derived; final `linarith` chains them. ✓

## Mathlib lemma name audit

All cited Mathlib symbols verified to exist with the asserted signatures:

| Symbol | Location | Verified signature |
|---|---|---|
| `Nat.mul_pos` | (used 9+ places in Mathlib) | `0 < a → 0 < b → 0 < a * b` |
| `Nat.le_mul_of_pos_right` | (used multiple places) | `(a : ℕ) → 0 < b → a ≤ a * b` |
| `Nat.le_mul_of_pos_left` | (used multiple places) | `(b : ℕ) → 0 < a → b ≤ a * b` |
| `one_div_le_one_div_of_le` | `Algebra/Order/Field/Basic.lean:69` | `0 < a → a ≤ b → 1/b ≤ 1/a` |
| `abs_add_le` | (used 10+ places) | `|a + b| ≤ |a| + |b|` |
| `div_le_div_iff₀` | `Algebra/Order/GroupWithZero/Unbundled/Basic.lean:1419` | `0 < b → 0 < d → (a/b ≤ c/d ↔ a*d ≤ c*b)` |
| `pow_le_pow_right₀` | `Algebra/Order/GroupWithZero/Unbundled/Basic.lean:446` | `1 ≤ a → m ≤ n → a^m ≤ a^n` |
| `Computable.succ` | `Computability/Partrec.lean:298` | `Computable Nat.succ` |
| `Computable.snd` | `Computability/Partrec.lean:288` | `Computable Prod.snd` |
| `Computable.const` | `Computability/Partrec.lean:271` | `(s : σ) → Computable (fun _ => s)` |
| `Computable.id` | `Computability/Partrec.lean:282` | `Computable id` |
| `Primrec.nat_mul` | `Computability/Primrec/Basic.lean:599` | `Primrec₂ ((· * ·) : ℕ → ℕ → ℕ)` |
| `Computable₂.comp` | `Computability/Partrec.lean:477` | `Computable₂ f → ... → Computable (fun a => f (g a) (h a))` |
| `Pi.add_apply` | (universal) | `(a + b) n = a n + b n` |
| `pow_succ` | (universal) | `a^(n+1) = a^n * a` |
| `IsComputableSeqRat.add` | `L1/ComputableSeqReal.lean:255` | as documented |
| `isComputableSeqReal_of_effectiveConvergence` | `L1/ComputableSeqReal.lean:157` | as documented |
| `ContinuousMap.add_apply` (via `@[to_additive]`) | `Topology/ContinuousMap/Algebra.lean:64` | `(f + g) x = f x + g x := rfl` |

All present, all matching. ✓

## P-R Definition A faithfulness

The proof closes `IsGLComputable` (Definition A) under `+`. P-R chapt0:504 explicitly states "$x \pm y$ … are computable over any computable rectangle on which they are continuous." This is the textbook authorization for sum-closure. The proof goes via the structural decomposition:
- Clause (i): pointwise sum of computable real sequences is computable (the helper `isComputableSeqReal_add`, which is a special case of P-R Ch. 0 Proposition 1 at chapt0:246 — closure of computable real sequences under effective limits).
- Clause (ii): effective uniform continuity is preserved under `+` via modulus product with `+1` shift and triangle inequality — this is the standard analysis argument.

No P-R departure or non-classical reasoning detected. ✓

## Concrete weaknesses (if any)

None substantive. Minor stylistic observations (not blocking):
1. The `h_eq` step (lines 308-313) is a `funext + show + rfl` triplet that could conceivably be just `rfl` directly (since the entire functional equality reduces by `rfl`-conversion through `ContinuousMap.add_apply` and `Pi.add_apply`). Not a correctness issue — defensive verbosity is fine.
2. The helper `isComputableSeqReal_add` is `private` and inlined in L2. The file docstring at lines 208-215 already documents that this is a TODO for L1 lift — appropriately disclosed, not a defect.
3. The `Nat.le_mul_of_pos_right _ h_dg_pos` idiom relies on Lean inferring the first explicit `ℕ` argument from the goal. This works (verified by clean build) but a reader might prefer named arguments. Stylistic only.

## Verdict justification

The proof is sound, complete, and faithfully formalizes P-R chapt0:504's closure of computable continuous functions under addition. The five-tuple unpacking, clause-(i) helper invocation, positivity of `d_sum` via `Nat.mul_pos`, the `+1` modulus shift cascade, the triangle inequality, and the `1/2^(N+1) + 1/2^(N+1) = 1/2^N` arithmetic all check out. Every cited Mathlib lemma exists with the asserted signature (verified by grep against `.lake/packages/mathlib/`). The axiom dependency is the minimal Mathlib base `[propext, Classical.choice, Quot.sound]` — no `sorryAx`. The iter-06 predicate fix (adding `∀ N, 0 < d N`) is correctly propagated and load-bearing for the positivity step; without it, the cascade would collapse, but no vacuity loophole is reintroduced. Three full attack rounds (citation, weakening, counterexample/edge) yielded no blocking issue.
